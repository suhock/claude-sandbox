services:
  claude:
    build:
      context: ${SANDBOX_ROOT}
      dockerfile: shared/Dockerfile
      args:
        BASE_IMAGE: mcr.microsoft.com/dotnet/sdk:10.0
        ENV_NAME: dotnet
    volumes:
      - ${HOME:-/dev/null}/.nuget/packages:/home/claude/.nuget/fallback-packages:ro
